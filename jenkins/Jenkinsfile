pipeline {
    agent any

    environment {
        IMAGE = "devsecops-app"
    }

    stages {

        stage('Build') {
            steps {
                sh 'docker build -t $IMAGE -f docker/Dockerfile .'
            }
        }

        stage('SonarQube') {
            steps {
                withSonarQubeEnv('sonar') {
                    sh '''
                    /opt/sonar-scanner-new/bin/sonar-scanner \
                      -Dproject.settings=sonar/sonar-project.properties
                    '''
                }
            }
        }

        stage('Trivy') {
            steps {
                sh 'trivy image $IMAGE'
            }
        }
    }
}